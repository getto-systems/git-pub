#!/bin/bash

git_wipe_widow_branch_main(){
  local refname

  for refname in $(git branch --format="%(refname)"); do
    if [ -z "$(git branch -r --list "pub/${refname#refs/heads/}")" ]; then
      git br -d "${refname#refs/heads/}"
    fi
  done
}

git_wipe_widow_branch_main "$@"
